// stereo_match.h: 标准系统包含文件的包含文件
// 或项目特定的包含文件。

#pragma once
#define _ABS(x) ((x) > 0 ? (x) : -(x))
#include <opencv2/opencv.hpp>

#include <stdio.h>
#include <math.h>
using namespace cv;
using namespace std;
// TODO: 在此处引用程序需要的其他标头。
